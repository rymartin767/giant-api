<div class="flex flex-col w-full bg-white">
    <div class="pl-2 text-2xl font-bold">{{ $description }}</div>
    <div class="pl-2 text-lg italic text-gray-500">{{ $endpoint }}</div>
    <div class="pl-2 text-sm text-blue-600">{{ $params }}</div>
    <div>
        {{ $torchlight }}
    </div>
</div>